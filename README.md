# React search movie trailer

A small React app for searching movie trailers

## Getting started

To run the app locally:

- Clone this repo
- `npm install` to install all required dependencies
- `npm start` to start the local server
